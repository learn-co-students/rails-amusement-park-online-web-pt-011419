Show <%= @attraction.name %>

<%= link_to "Edit Attraction", edit_attraction_path if @user.admin%>

<%= form_for([@attraction, @user]) do |f| %>
  <%= f.submit 'Go on this ride' %>
<% end %>
<%= link_to 'Other way to do this', attraction_user_path(@attraction)%>