<% @attractions.each do |a| %>
    <%= a.name %>:<br>
    <%= link_to "Go on #{a.name}", attraction_path(a) if !@user.admin %>
    <%= link_to "Show #{a.name}", attraction_path(a) if @user.admin %>
    <br><br>
<% end %>


<%= link_to "New Attraction", new_attraction_path if @user.admin %>